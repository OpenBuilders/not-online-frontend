# Local Nest backend. Set the deployed backend URL before the production build.
LINK_PAGES_API_URL=http://127.0.0.1:3000

# Self-hosted Plausible CE. Both values are public browser configuration;
# leave either blank to disable analytics in the public renderer.
PUBLIC_PLAUSIBLE_HOST=
PUBLIC_PLAUSIBLE_DOMAIN=
