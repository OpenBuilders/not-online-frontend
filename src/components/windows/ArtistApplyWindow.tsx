import { ComingSoon } from '@/components/windows/ComingSoon';

/** Placeholder — will port openArtistApply() (Tools.html:2882-2908). */
export function ArtistApplyWindow() {
  return <ComingSoon name="Apply as Artist" />;
}
