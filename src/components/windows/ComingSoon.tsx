/** Temporary placeholder for window content not built yet — swapped out feature by feature. */
export function ComingSoon({ name }: { name: string }) {
  return <div style={{ padding: 24, color: 'var(--c-ink)' }}>{name} — coming in a later stage.</div>;
}
