import { ComingSoon } from '@/components/windows/ComingSoon';

/** Placeholder — will port openPatron() (Tools.html:2469-2498). */
export function PatronWindow() {
  return <ComingSoon name="become a patron of nothing" />;
}
