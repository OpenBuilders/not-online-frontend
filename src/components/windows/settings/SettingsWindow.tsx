import { ComingSoon } from '@/components/windows/ComingSoon';

/** Placeholder — will port openSettings() (Tools.html:3060-3185). */
export function SettingsWindow() {
  return <ComingSoon name="Settings" />;
}
